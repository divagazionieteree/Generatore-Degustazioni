# Comando unico per installare tutte le dipendenze da zero su Google Colab
# Copia e incolla questo comando in una cella del notebook
# Installa tutto partendo da un ambiente pulito

!pip install -q --upgrade pip && \
pip install -q --upgrade --force-reinstall numpy>=2.0 && \
pip install -q --upgrade --force-reinstall pandas && \
pip install -q --upgrade \
  torch \
  transformers==4.44.2 \
  peft==0.12.0 \
  accelerate==0.33.0 \
  datasets \
  bitsandbytes \
  sentencepiece \
  fsspec \
  matplotlib \
  scikit-learn \
  plotly

